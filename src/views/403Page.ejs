<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>403 - Forbidden Access</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #cb2d3e 0%, #ef473a 100%);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            overflow: hidden;
        }

        .error-container {
            text-align: center;
            max-width: 800px;
            padding: 40px;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            animation: shakeX 1.5s;
            position: relative;
        }

        .error-code {
            font-size: 150px;
            font-weight: 900;
            margin-bottom: 20px;
            text-shadow: 2px 2px 15px rgba(0, 0, 0, 0.3);
            position: relative;
            display: inline-block;
        }

        .error-code::after {
            content: '403';
            position: absolute;
            left: 5px;
            top: 5px;
            color: rgba(255, 255, 255, 0.2);
            z-index: -1;
        }

        .error-title {
            font-size: 32px;
            margin-bottom: 15px;
            color: #fff;
        }

        .error-message {
            font-size: 18px;
            margin-bottom: 30px;
            color: rgba(255, 255, 255, 0.9);
            line-height: 1.6;
        }

        .back-home {
            display: inline-block;
            padding: 15px 30px;
            background: rgba(255, 255, 255, 0.2);
            color: #fff;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        .back-home:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .lock-icon {
            font-size: 80px;
            margin-bottom: 30px;
            color: rgba(255, 255, 255, 0.9);
            animation: bounce 2s infinite;
        }

        @keyframes shakeX {

            0%,
            100% {
                transform: translateX(0);
            }

            10%,
            30%,
            50%,
            70%,
            90% {
                transform: translateX(-10px);
            }

            20%,
            40%,
            60%,
            80% {
                transform: translateX(10px);
            }
        }

        @keyframes bounce {

            0%,
            20%,
            50%,
            80%,
            100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-20px);
            }

            60% {
                transform: translateY(-10px);
            }
        }

        .shield {
            position: absolute;
            opacity: 0.1;
            font-size: 220px;
            z-index: -1;
        }

        .shield-1 {
            top: -50px;
            left: -50px;
            transform: rotate(-25deg);
        }

        .shield-2 {
            bottom: -50px;
            right: -50px;
            transform: rotate(25deg);
        }
    </style>
</head>

<body>
    <div class="error-container">
        <i class="fas fa-shield-alt shield shield-1"></i>
        <i class="fas fa-shield-alt shield shield-2"></i>
        <i class="fas fa-lock lock-icon"></i>
        <div class="error-code">403</div>
        <h1 class="error-title">Access Forbidden</h1>
        <p class="error-message">
            <%= typeof message !=='undefined' ? message : 'Sorry, you don\' t have permission to access this area.' %>
        </p>
        <p class="error-message">
            If you believe this is a mistake, please contact the administrator.
        </p>
        <a href="<%= typeof returnUrl !== 'undefined' ? returnUrl : '/' %>" class="back-home">
            <i class="fas fa-home"></i> Return to Safety
        </a>
    </div>
</body>

</html>